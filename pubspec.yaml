name: smart_alarm
description: 智能闹钟提醒应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.0.0
  android_alarm_manager_plus: ^4.0.0
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.2
  file_picker: ^6.1.1
  path_provider: ^2.1.0
  permission_handler: ^11.0.1
  intl: ^0.18.1
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
